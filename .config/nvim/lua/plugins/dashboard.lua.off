return {
	{
		"nvimdev/dashboard-nvim",
		name = "dashboard",
		event = "VimEnter",
		dependencies = { { "nvim-tree/nvim-web-devicons" } },
		config = function()
			require("dashboard").setup({
				theme = "hyper",
				confirm_key = "p",
				shortcut_type = "number",
				shuffle_letter = false,
				change_to_vcs_root = true,
				hide = {
					statusline = true,
					tabline = true,
					winbar = true,
				},
				preview = {
					-- command = "test",
					-- file_path = "testest",
					-- file_height = 25,
					-- file_width = 25,
				},
				config = {
					-- header = { "asdasd" },
					week_header = {
						enable = true,
						concat = os.date("%A"),
					},
					disable_move = true,
					packages = { enable = true },
					project = {
						enable = true,
						limit = 10,
						icon = " ",
						action = "Telescope find_files cwd=",
						label = " Recent Projects:",
					},
					mru = {
						enable = true,
						limit = 16,
						icon = " ",
						label = " Most Recent Files:",
						cwd_only = false,
					},
					shortcut = {
						{ desc = "󰒲 Lazy", action = "Lazy", key = "l" },
						{ desc = " Mason", action = "Mason", key = "m" },
						{ desc = " .dotfiles", action = "Telescope .dotfiles", key = "d", group = "Comment" },

						-- {
						-- 	icon = " ",
						-- 	desc = "Files",
						-- 	group = "Label",
						-- 	action = "Telescope find_files",
						-- 	key = "f",
						-- },
					},
				},
			})
		end,
		keys = {
			{ "<leader>db", ":Dashboard<CR>", { "n" }, desc = "[d]ash[b]oard" },
		},
	},
}
